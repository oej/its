(DEFUN IN-PACKAGE (&REST X)
  NIL)

(DEFVAR :USE ':USE)

(DEFUN SHADOW (X)
  NIL)

(DEFUN EXPORT (X)
  NIL)

(DEFMACRO DEFCONSTANT (&REST X)
  `(DEFVAR ,@X))

(DEFUN CODE-CHAR (X)
  X)

(DEFVAR *TERMINAL-IO* NIL)

(PRINC '(WE SHOULDNT PUSH TO THE FRONT))
(SETQ /#-SYMBOLIC-CHARACTERS-TABLE
      (LIST* '(PAGE . 12.)
       	     '(# . 35.)
	     /#-SYMBOLIC-CHARACTERS-TABLE))

(DEFUN CONCATENATE (TYPE X Y)
  NIL)
